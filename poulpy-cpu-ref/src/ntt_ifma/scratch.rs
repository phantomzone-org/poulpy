//! Scratch (temporary) memory management for [`NTTIfmaRef`](crate::NTTIfmaRef).
//!
//! Now provided by shared `poulpy-hal` portable defaults via the `HalScratchDefaults`
//! trait and the `hal_impl_scratch!()` macro.
